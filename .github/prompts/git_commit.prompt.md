---
name: git-commit
description: "Review the working tree and create a commit for the intended changes"
agent: agent
---

Create a new commit for the intended uncommitted changes. Run `git status`, `git diff HEAD`, and `git status --porcelain` to review the working tree before adding changed and untracked files.